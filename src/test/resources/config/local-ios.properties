# Used only when EXECUTION_TARGET=local (simulator with a local Appium server).
# On BrowserStack these are ignored; capabilities come from browserstack-ios.yml.

appium.server.url=http://127.0.0.1:4723
deviceName=iPhone 15
platformVersion=17
app=./apps/Wikipedia.app
bundleId=org.wikimedia.wikipedia
newCommandTimeout=120
